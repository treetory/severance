//
// Built on Thu Nov 07 05:36:54 CET 2019 by logback-translator
// For more information on configuration files in Groovy
// please see http://logback.qos.ch/manual/groovy.html

// For assistance related to this tool or configuration files
// in general, please contact the logback user mailing list at
//    http://qos.ch/mailman/listinfo/logback-user

// For professional support please see
//   http://www.qos.ch/shop/products/professionalSupport
/*
import ch.qos.logback.classic.encoder.PatternLayoutEncoder
import java.nio.charset.Charset

debug(true)
scan("30 seconds")

def HOSTNAME = hostname

appender("consoleAppender", ConsoleAppender) {
  encoder(PatternLayoutEncoder) {
    charset = Charset.forName("UTF-8")
      pattern = "▶ %-5level %d{HH:mm:ss.SSS} [${HOSTNAME}] [%thread] %logger{36} - %msg%n"

  }
}

logger("org.springframework", WARN, ["consoleAppender"])
logger("com.treetory.severance", DEBUG, ["consoleAppender"])
logger("com.treetory.severance.FileController", DEBUG, ["consoleAppender"])
root(DEBUG, ["consoleAppender"])
 */